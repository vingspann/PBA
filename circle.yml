dependencies:
  override:
    - nvm install 6
    - npm install
    - npm install webpack -g
    - npm i prop-types
    - webpack

deployment:
  production:
    branch: master
    heroku:
      appname: pokemon-battling-application