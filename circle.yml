dependencies:
  override:
    - nvm install 6
    - npm install
    - npm install webpack -g
    - webpack --display-error-details

deployment:
  production:
    branch: master
    heroku:
      appname: pokemon-battling-application